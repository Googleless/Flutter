name: kt2  # Название вашего приложения (только строчные буквы и подчеркивания)
description: A simple ToDo app built with Flutter.

# Версия приложения (необязательно, но полезно для публикации)
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"  # Минимальная версия Dart (Flutter 3.0+ использует Dart 3)

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.5  # Для работы с файловой системой
  intl: ^0.18.1          # Для форматирования дат (опционально)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0  # Для анализа кода (рекомендуется)